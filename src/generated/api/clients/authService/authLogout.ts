/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Options, RequestResult } from '../../.kubb/client';
import type { AuthLogoutOptions, AuthLogoutResponses } from '../../models/Auth/AuthLogout';
import { client } from '../../.kubb/client';
import {
  authLogoutResponseSchema,
  authLogoutErrorSchema,
} from '../../schemas/Auth/authLogoutSchema';

/**
 * @description Revoga a família de refresh token enviada no corpo — outras sessões do mesmo usuário não são afetadas. Para logout em todos os dispositivos, ver `DELETE /auth/sessions`.
 * Não exige access token válido (RFC 7009 — endpoints de revogação não devem depender de credencial ainda ativa): a fonte primária de revogação é o refresh token, não o access token. Se o header `Authorization` vier com um access token ainda válido, seu `jti` também é bloqueado no DragonflyDB como defesa extra; ausente/expirado/ inválido não impede o logout. Resposta sempre 200, mesmo com refresh token desconhecido ou já revogado — idempotente, sem vazar validade do token. Ver ADR-0023.
 * @summary Logout (dispositivo atual)
 * {@link /auth/logout}
 */
export function authLogout<ThrowOnError extends boolean = true>(
  options: Options<AuthLogoutOptions, ThrowOnError>
): Promise<RequestResult<AuthLogoutResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options;

  return request({
    method: 'DELETE',
    url: '/auth/logout',
    security: [{ type: 'http', scheme: 'bearer' }],
    validator: { response: authLogoutResponseSchema, error: authLogoutErrorSchema },
    ...config,
  }) as Promise<RequestResult<AuthLogoutResponses, ThrowOnError>>;
}
