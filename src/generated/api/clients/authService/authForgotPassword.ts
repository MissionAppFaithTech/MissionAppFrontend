/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Options, RequestResult } from '../../.kubb/client';
import type {
  AuthForgotPasswordOptions,
  AuthForgotPasswordResponses,
} from '../../models/Auth/AuthForgotPassword';
import { client } from '../../.kubb/client';
import {
  authForgotPasswordResponseSchema,
  authForgotPasswordErrorSchema,
} from '../../schemas/Auth/authForgotPasswordSchema';

/**
 * @description Envia um email com link de redefinição de senha, se o login informado corresponder a um usuário cadastrado. Sempre responde 200 com a mesma mensagem genérica, exista ou não o email — evita user enumeration (não revela se um endereço está cadastrado). O link expira em 60 minutos.
 * @summary Solicitar redefinição de senha
 * {@link /auth/forgot-password}
 */
export function authForgotPassword<ThrowOnError extends boolean = true>(
  options: Options<AuthForgotPasswordOptions, ThrowOnError>
): Promise<RequestResult<AuthForgotPasswordResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options;

  return request({
    method: 'POST',
    url: '/auth/forgot-password',
    validator: { response: authForgotPasswordResponseSchema, error: authForgotPasswordErrorSchema },
    ...config,
  }) as Promise<RequestResult<AuthForgotPasswordResponses, ThrowOnError>>;
}
